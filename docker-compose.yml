version: "3.9"

services:
  web:
    build: .
    container_name: engine_app
    ports:
      - "8000:8000"
    restart: always

    # ✅ .env file load
    env_file:
      - .env

    # ✅ optional (good for logs)
    environment:
      - PYTHONUNBUFFERED=1
